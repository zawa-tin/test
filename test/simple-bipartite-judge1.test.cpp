#define PROBLEM "https://onlinejudge.u-aizu.ac.jp/courses/lesson/2/ITP1/1/ITP1_1_A"

#include "../src/graph/simple/bipartiteJudge.hpp"

#include <iostream>
#include <cassert>

int main() {
 //    int n, m; std::cin >> n >> m;
 //    std::vector G1(n, std::vector(0, 0));
 //    std::vector G2(n, std::vector(0, std::pair(0, 0)));
 //    for (int _ = 0 ; _ < m ; _++) {
 //        int a, b; std::cin >> a >> b;
 //        G1[a].push_back(b);
 //        G1[b].push_back(a);
 //        G2[a].push_back({ b, 1 });
 //        G2[b].push_back({ a, 1 });
 //    }
	// zawa::bipartiteJudge c1(G1);
	// zawa::bipartiteJudge c2(G2);
 //    assert(c1.ok() == c2.ok());
	// std::cout << (c1.ok() ? "Yes" : "No") << std::endl;
    
    std::cout << "Hello World" << std::endl;
}

/*
 * アルゴ式 グラフアルゴリズム5章Q4. 二部グラフ判定(D)
 * https://algo-method.com/submissions/873881
 */
