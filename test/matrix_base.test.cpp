#define PROBLEM "https://onlinejudge.u-aizu.ac.jp/courses/lesson/2/ITP1/1/ITP1_1_A"
#include "../src/math/matrix/matrix_base.hpp"

// constexpr long long mod = 1000000007;
// 
// struct semiring {
// 
// using T = long long;
// static constexpr T e = 0;
// static T add(const T& a, const T& b) {
// 	return (a + b) % mod;
// }
// static T mul(const T& a, const T& b) {
// 	return (a * b) % mod;
// }
// static T inv(const T& a) {
// 	return (a == e ? a : mod - a);
// }
// 
// };
// 
// using mtx = zawa::matrix<semiring>;

#include <iostream>

int main() {
	// int n; std::cin >> n;
	// long long k; std::cin >> k;
	// mtx G(n, n);
	// for (int i = 0 ; i < n ; i++) {
	// 	for (int j = 0 ; j < n ; j++) {
	// 		std::cin >> G[i][j];
	// 	}
	// }
	// std::vector elem(n, std::vector(n, 0LL));
	// for (int i = 0 ; i < n ; i++) {
	// 	elem[i][i] = 1LL;
	// }
	// zawa::matrix_util::e<semiring> = mtx(elem);
	// G = zawa::pow(G, k);
	// long long ans = 0LL;
	// for (int i = 0 ; i < n ; i++) {
	// 	for (int j = 0 ; j < n ; j++) {
	// 		ans += G[i][j];
	// 		ans %= mod;
	// 	}
	// }
	// std::cout << ans << std::endl;
	std::cout << "Hello World" << std::endl;
}

/*
 * Educational DP Contest - R Walk
 * https://atcoder.jp/contests/dp/submissions/37964556
 */
